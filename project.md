# JobPilot 项目说明

## 项目概述

JobPilot 是一个完整的智能求职助手系统，整合了简历解析、智能优化、自动投递和进度管理四大核心功能，为求职者提供从简历准备到投递跟踪的全流程自动化支持。

## 快速开始

### 一键启动

```bash
# 1. 进入项目目录
cd /Users/zijiancai/Desktop/hkucsfiles/comp7607/JobPilot/project_eng/backend

# 2. 运行启动脚本
./start.sh

# 3. 打开浏览器访问
# http://localhost:5000
```

### 环境要求

- **Python**: 3.8 或更高版本
- **浏览器**: Chrome、Firefox、Safari 等现代浏览器
- **Java**（可选）: 用于自动投递功能，需要 Java 21+

## 功能入口

访问 http://localhost:5000 后，您将看到四个主要功能标签：

### 1. 简历解析
**位置**：首页第一个标签

**功能**：
- 上传 PDF、DOCX 或 TXT 格式的简历
- 自动提取个人信息、教育背景、工作经历、技能等
- 查看结构化的简历数据

**使用方法**：
1. 点击"选择文件"上传简历
2. 点击"上传并解析"
3. 等待解析完成，查看结构化信息
4. 点击列表中的简历可查看详情

### 2. 简历优化
**位置**：首页第二个标签

**功能**：
- 基于目标职位描述（JD）分析简历匹配度
- 智能重排经历顺序，突出相关内容
- 使用AI优化经历描述
- 生成针对性的PDF简历

**使用方法**：
1. 从下拉菜单选择已上传的简历
2. 粘贴目标职位的JD到文本框
3. 点击"分析匹配度"查看相关性分析
4. 点击"生成优化简历"创建定制化简历
5. 点击"下载PDF简历"获取文件

### 3. 自动投递
**位置**：首页第三个标签

**功能**：
- 自动在Boss直聘、猎聘等平台投递简历
- 智能搜索和筛选职位
- 自动填写表单
- 实时显示投递进度

**使用方法**：
1. 选择要投递的简历
2. 选择投递平台（Boss直聘/猎聘）
3. 配置搜索关键词和目标城市
4. 点击"启动投递"
5. 首次使用需在弹出的浏览器窗口中手动登录
6. 系统将自动投递，遇到验证码时需手动处理

**注意**：
- 自动投递功能需要Java运行环境
- 建议先优化简历后再投递
- 投递过程中不要关闭浏览器窗口

### 4. 进度管理
**位置**：首页第四个标签

**功能**：
- 记录所有投递信息
- 多状态跟踪（已申请、笔试、面试、offer等）
- 可视化时间线展示
- 统计分析投递数据

**使用方法**：
1. 点击"+ 添加新投递"手动添加记录
2. 填写公司名称、职位名称等信息
3. 查看所有投递记录和状态
4. 点击记录可更新状态
5. 查看统计信息

## 推荐工作流程

```
步骤1: 简历解析
└─ 上传原始简历
   └─ 查看解析结果

步骤2: 简历优化
└─ 选择简历
   └─ 输入目标JD
      └─ 生成优化版本

步骤3: 自动投递（可选）
└─ 配置投递参数
   └─ 启动自动投递
      └─ 监控进度

步骤4: 进度管理
└─ 添加/查看投递记录
   └─ 更新状态
      └─ 统计分析
```

## 核心技术

### 后端
- **Flask**: Web应用框架
- **SQLite**: 轻量级数据库
- **阿里云千问**: 大语言模型（简历解析、内容优化）
- **Playwright**: 浏览器自动化

### 前端
- **HTML/CSS/JavaScript**: 原生技术栈
- **响应式设计**: 适配各种屏幕

### AI能力
- **简历解析**: qwen-plus 模型
- **简历优化**: qwen3-coder-flash 模型
- **相似度计算**: text-embedding-v3 向量模型

## 项目结构

```
project_eng/
├── backend/              # 后端代码
│   ├── app.py           # 主应用入口
│   ├── modules/         # 功能模块
│   ├── data/            # 数据存储
│   ├── start.sh         # 启动脚本
│   └── requirements.txt # 依赖列表
└── frontend/            # 前端代码
    ├── index.html      # 主页面
    ├── css/            # 样式文件
    └── js/             # 脚本文件
```

## 常见问题

### Q1: 如何停止服务？
**A**: 在终端按 `Ctrl+C`

### Q2: 端口被占用怎么办？
**A**: 修改 `backend/app.py` 最后一行的端口号（默认5000）

### Q3: 简历解析失败？
**A**:
- 确保文件格式正确（PDF、DOCX、TXT）
- 检查网络连接（需要调用AI API）
- 查看终端错误信息

### Q4: 简历优化很慢？
**A**: 这是正常现象，生成优化简历需要1-2分钟，请耐心等待

### Q5: 自动投递无法使用？
**A**:
- 确保安装了Java 21+
- 检查Func2_AutoApplication目录是否存在
- 该功能为包装层，完整功能需要配置Java服务

### Q6: 数据存储在哪里？
**A**: 所有数据存储在 `backend/data/` 目录：
- `resumes/` - 上传的简历
- `generated/` - 生成的简历
- `database/` - SQLite数据库

## 数据安全

- ✅ 所有简历数据存储在本地
- ✅ 不会上传到第三方服务器
- ✅ LLM调用使用安全的API
- ⚠️ 建议定期备份 `data` 目录

## 扩展与定制

### 更换AI模型
编辑以下文件中的配置：
- `backend/modules/resume_parser/parser.py`
- `backend/modules/resume_optimizer/optimizer.py`

### 修改界面样式
编辑：`frontend/css/style.css`

### 添加新功能
1. 在 `backend/modules/` 创建新模块
2. 在 `backend/app.py` 注册蓝图
3. 更新前端界面

## 技术支持

如遇到问题：
1. 查看终端输出的错误信息
2. 检查 `backend/README.md` 中的详细说明
3. 确保所有依赖已正确安装

## 项目来源

本项目整合了以下四个独立功能模块：
- **Func1_Parser** - 简历解析
- **Func2_AutoApplication** - 自动投递
- **Func3_Optimizer** - 简历优化
- **Func4_StatusTracking** - 进度管理

通过统一的Web界面和后端API实现了完整的功能整合。

---

**开始使用 JobPilot，让求职更高效！** 🚀
